services:
  app:
    network_mode: host
    extra_hosts:
      - "host.docker.internal:127.0.0.1"
    build:
      context: .
      dockerfile: ./php/Dockerfile
      target: "${BUILD_TARGET:-app}"
    volumes:
      - ./app:/var/www/html